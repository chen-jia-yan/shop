# 任务：批处理第二批加固「护栏」（重/轻分流 + 准入上限 + 单文件超时 + 落盘日志 + 全局限流）

## 背景与约束
本地日文保险知识库 ingest：worker.py 用 ProcessPoolExecutor（已做并发=4、max_tasks_per_child、BrokenProcessPool 自愈、毒丸隔离）；supervisor.py 已监控内存/存活/回收卡死/写 health；父进程批量写 Azure AI Search + embedding。第一批止血已完成。现在加护栏，进一步防止重文件拖垮系统、任务卡死、下游被限流。
硬约束：只用标准库 + 现有依赖，禁止新增 pip 包；Windows；不破坏现有状态机与自愈逻辑；先输出计划和 diff，我确认后再实现。

## 改动 1：重/轻文件分流 + 重任务准入上限
- 入队或领取时给每个文件估算"重量"：文件大小、（PPT/PDF）页数、内嵌图片数，超阈值判为"重文件"（阈值设为顶部常量，先给保守值）。
- 领取策略：轻文件正常并发；重文件走低并发。用一个"在处理的重任务计数"限制同时进行的重任务数 ≤ HEAVY_LIMIT（如 2）。dispatcher 每轮领取时，保证在飞重任务不超过该上限，宁可先领轻文件。
- 目的：避免多个大 Excel/多图 PPT 同时并发把内存顶爆。

## 改动 2：单文件超时（Windows 现实做法）
- 给每个文件设处理超时（按重量分级，如轻 2 分钟 / 重 8 分钟，顶部常量）。
- 说明现实约束：Windows 下无法干净地强杀进程池里的单个任务。所以采用组合式超时：
  1. dispatcher 记录每个 future 的提交时间；超过其超时上限的，标记该文件为 failed(error='timeout')、attempts+1，不再等它。
  2. 依赖已有的 max_tasks_per_child 回收 + supervisor 的卡死回收（reset_stuck_timeout）最终释放掉那个卡住的 worker/任务。
  3. 若超时任务导致 worker 长期占用，允许在超时后主动重建进程池（复用 BrokenProcessPool 那套 new_pool）。
- 目的：任何单个文件都不能无限占用 worker、卡死整批进度。

## 改动 3：落盘结构化日志（别再靠系统事件日志倒推）
- worker 和 supervisor 用标准库 logging + RotatingFileHandler，各写自己的日志文件（如 logs/worker.log、logs/supervisor.log，单文件上限+滚动备份）。
- 每个文件至少记录：doc_id/文件名、开始时间、结束时间、耗时、最终状态、错误堆栈（失败时）、是否重文件。
- 关键事件也记：进程池重建、毒丸隔离、超时、内存背压触发、限流退避。
- 目的：出问题能直接看日志定位，不用再从 Windows 事件日志逆向。

## 改动 4：Azure / embedding 全局限流
- 在父进程加一个全局限流器（标准库实现，简单令牌桶或"最小请求间隔"即可），所有对 Azure AI Search 写入和 embedding 的调用都过它，控制整体每秒请求数上限，避免多任务汇聚把下游打到 429。
- 与已有的 429/503 指数退避配合：限流负责"平时不超速"，退避负责"真被限流时让步"。
- 目的：并发上来时下游稳定，不因限流导致整批失败。

## 验收标准（请给出验证步骤）
1. 同时投入多个大文件：在飞重任务数不超过 HEAVY_LIMIT，内存平稳。
2. 放一个会卡很久的文件：超时后被标 failed(timeout)，整批继续，卡住的 worker 最终被回收/池重建。
3. logs/ 下能看到每个文件的处理记录和关键事件。
4. 高并发写 Azure/embedding 时，请求速率被限流器压在上限内，不再频繁 429。
先输出：改动文件清单 + 关键 diff（重量估算与分流、重任务计数、超时处理、RotatingFileHandler、全局限流器），确认后再实现。


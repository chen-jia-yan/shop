# 业务流程图

> AI-Powered Intelligent Testing Platform 的核心业务流程

---

## 1. 端到端测试流程 — 从需求分析到报告生成

```mermaid
flowchart TB
    START(["📥 上传需求文档"]) --> PARSE["需求解析<br/>Azure OpenAI 提取功能点"]
    PARSE --> GEN_CASE["测试用例生成<br/>Azure OpenAI + AI Search RAG"]
    GEN_CASE --> REVIEW["人工评审<br/>Power Automate 审批流"]
    REVIEW --> APPROVED{"评审通过?"}
    APPROVED -->|是| GEN_SCRIPT["自动化脚本生成<br/>Playwright / Selenium"]
    APPROVED -->|否| FEEDBACK["根据反馈修改"] --> GEN_CASE
    GEN_SCRIPT --> EXEC["测试执行<br/>Playwright Engine"]
    EXEC --> ANALYZE{"执行结果"}
    ANALYZE -->|全部通过| REPORT["生成测试报告"]
    ANALYZE -->|部分失败| FAILURE["AI 失败分析<br/>根因定位"]
    FAILURE --> CLASSIFY["失败分类<br/>(环境/脚本/产品Bug)"]
    CLASSIFY -->|产品 Bug| CREATE_BUG["自动创建 Bug<br/>DevOps / Jira"]
    CLASSIFY -->|脚本问题| FIX_SCRIPT["自动修复脚本"] --> RE_EXEC["重新执行"]
    CLASSIFY -->|环境问题| NOTIFY["通知运维<br/>Teams 推送"]
    CREATE_BUG --> REPORT
    FIX_SCRIPT --> REPORT
    NOTIFY --> REPORT
    RE_EXEC --> ANALYZE
    REPORT --> DISTRIBUTE["报告分发<br/>Teams + Outlook"]
    DISTRIBUTE --> LEARN["AI 学习更新<br/>失败模式写入 AI Search"]
    LEARN --> END(["✅ 完成"])

    classDef start fill:#c8e6c9,stroke:#2e7d32
    classDef process fill:#e3f2fd,stroke:#1565c0
    classDef decision fill:#fff9c4,stroke:#f9a825
    classDef fail fill:#ffccbc,stroke:#d84315
    classDef end fill:#fce4ec,stroke:#c2185b

    class START start
    class PARSE,GEN_CASE,GEN_SCRIPT,RE_EXEC process
    class APPROVED,ANALYZE,CLASSIFY decision
    class FEEDBACK,FAILURE,FIX_SCRIPT,NOTIFY,CREATE_BUG fail
    class REPORT,DISTRIBUTE,LEARN,END end
```

### 端到端流程角色矩阵

| 步骤 | 触发 | 执行方 | 输出 | SLA |
|------|------|--------|------|-----|
| 上传需求 | 用户操作 | Power Apps → SharePoint | 需求文档存储 | < 5s |
| 需求解析 | 上传完成 | Azure Functions + OpenAI | 结构化功能点 | < 30s |
| 用例生成 | 解析完成 | Azure Functions + OpenAI + AI Search | 测试用例列表 | < 60s |
| 人工评审 | 用例就绪 | Power Automate → Teams | 评审记录 | < 4h |
| 脚本生成 | 评审通过 | Azure Functions + OpenAI | 自动化脚本 | < 60s |
| 测试执行 | 脚本就绪 | Playwright / Selenium | 执行结果 | 按用例数 |
| 失败分析 | 执行完成 | Azure Functions + OpenAI | 失败分析报告 | < 30s |
| 报告生成 | 所有完成 | Azure Functions | 测试报告 | < 10s |

---

## 2. 需求解析流程

```mermaid
flowchart TB
    START(["📤 用户上传需求文件"]) --> CHECK{"文件格式"}
    CHECK -->|PDF| PDF_PROC["PDF 解析<br/>Azure Form Recognizer"]
    CHECK -->|Word| WORD_PROC["Word 解析<br/>Open XML SDK"]
    CHECK -->|Markdown| MD_PROC["Markdown 解析<br/>直接提取"]
    CHECK -->|其他| ERROR["📛 格式不支持<br/>提示用户重新上传"]

    PDF_PROC --> TEXT["提取纯文本内容"]
    WORD_PROC --> TEXT
    MD_PROC --> TEXT

    TEXT --> SPLIT["文档分段<br/>(章节/标题/段落)"]
    SPLIT --> CHUNK["意图分块<br/>每块 ≤ 4K tokens"]

    CHUNK --> EMBED["向量化<br/>text-embedding-3-large"]
    EMBED --> STORE_EMBED["向量存入 AI Search<br/>requirement-index"]

    CHUNK --> PROMPT["组装分析 Prompt<br/>系统模板 + 分块内容"]
    PROMPT --> FEATURES["GPT-4o 提取功能点<br/>Function Calling 结构化"]
    FEATURES --> BIZ_RULES["提取业务规则"]
    FEATURES --> EDGE_CASES["提取边界条件"]
    FEATURES --> DATA_REQ["提取数据约束"]
    FEATURES --> UI_INFO["提取界面元素"]

    FEATURES --> MERGE["合并去重<br/>同一需求的多个功能点"]
    MERGE --> SAVE["保存需求实体<br/>Dataverse Requirement"]
    SAVE --> NOTIFY["通知测试工程师<br/>Teams 推送"]

    START2(["📤 用户通过 Copilot Studio 对话上传需求"]) --> COPILOT["Copilot 对话引导<br/>提取关键信息"]
    COPILOT --> SPLIT

    ERROR --> END_ERROR(["❌ 结束"])
    NOTIFY --> END(["✅ 完成"])

    classDef start fill:#c8e6c9,stroke:#2e7d32
    classDef process fill:#e3f2fd,stroke:#1565c0
    classDef decision fill:#fff9c4,stroke:#f9a825
    classDef error fill:#ffccbc,stroke:#d84315
    classDef ai fill:#e1bee7,stroke:#6a1b9a

    class START,START2 start
    class PDF_PROC,WORD_PROC,MD_PROC,TEXT,SPLIT,CHUNK,PROMPT,MERGE,SAVE,NOTIFY,COPILOT process
    class CHECK decision
    class ERROR,END_ERROR error
    class EMBED,STORE_EMBED,FEATURES,BIZ_RULES,EDGE_CASES,DATA_REQ,UI_INFO ai
```

### 需求解析输出结构

```json
{
  "requirementId": "REQ-2026-001",
  "title": "用户登录模块",
  "parsed": {
    "features": [
      {
        "id": "F001",
        "name": "用户名密码登录",
        "description": "用户可使用注册邮箱和密码登录系统",
        "priority": "P0",
        "dependsOn": []
      }
    ],
    "businessRules": [
      {
        "rule": "密码连续输错5次后账户锁定30分钟",
        "source": "安全需求章节第3段"
      }
    ],
    "edgeCases": [
      "用户名为空时点击登录",
      "密码含特殊字符如 SQL 注入语句"
    ],
    "dataConstraints": [
      "用户名：3-50字符，支持邮箱格式",
      "密码：8-20字符，必须含大小写字母和数字"
    ],
    "uiElements": ["登录按钮", "用户名输入框", "密码输入框", "忘记密码链接"]
  }
}
```

---

## 3. 用例生成与评审流程

```mermaid
flowchart TB
    START(["🚀 功能点就绪"]) --> LOAD["加载需求功能点<br/>从 Dataverse"]

    LOAD --> SEARCH["AI Search 检索<br/>相似历史用例 / 常见场景"]
    SEARCH --> ASSEMBLE["组装生成 Prompt<br/>需求上下文 + 历史参考 + 用例模板"]

    ASSEMBLE --> GEN["GPT-4o 批量生成用例<br/>Function Calling 结构输出"]

    GEN --> VALIDATE{"格式校验"}
    VALIDATE -->|通过| DEDUP["去重检查<br/>对比已有用例"]
    VALIDATE -->|不通过| RETRY["自动重试<br/>最多 3 次"] --> GEN

    DEDUP --> CLASSIFY_CASE["用例分类<br/>功能/边界/异常/性能"]
    CLASSIFY_CASE --> ADD_TAG["打标签<br/>(模块/优先级/风险等级)"]
    ADD_TAG --> SAVE_CASE["保存用例<br/>Dataverse TestCase<br/>状态：待评审"]

    SAVE_CASE --> START_REVIEW["发起评审<br/>Power Automate 审批流"]
    START_REVIEW --> ASSIGN["分配评审人<br/>(按模块自动分配)"]
    ASSIGN --> PUSH["Teams 推送审批卡<br/>Adaptive Card"]
    PUSH --> WAIT["⏳ 等待评审"]

    WAIT --> APPROVE_REVIEW{"评审结果"}
    APPROVE_REVIEW -->|通过| APPROVED_STATUS["状态更新：已确认<br/>进入脚本生成阶段"]
    APPROVE_REVIEW -->|需修改| MODIFY["评审人标注修改意见"]
    MODIFY --> FEEDBACK_CASE["写入修改反馈<br/>ReviewRecord"]
    FEEDBACK_CASE ----> GEN["GPT-4o 根据反馈重新生成"]
    APPROVE_REVIEW -->|拒绝| REJECT["评审人填写拒绝原因"]
    REJECT --> CLOSE_CASE["状态更新：已关闭<br/>记录拒绝原因"]
    APPROVE_REVIEW -->|批量通过| BATCH_APPROVE["批量确认<br/>状态更新：已确认"]

    MODIFY --> DETAIL{"是否批量处理"}
    DETAIL -->|单条| SINGLE_FIX["单条修改后重生成"]
    DETAIL -->|多条| BATCH_FIX["批量修正后<br/>合并重生成"]

    classDef start fill:#c8e6c9,stroke:#2e7d32
    classDef process fill:#e3f2fd,stroke:#1565c0
    classDef ai fill:#e1bee7,stroke:#6a1b9a
    classDef decision fill:#fff9c4,stroke:#f9a825
    classDef review fill:#fff3e0,stroke:#e65100
    classDef endState fill:#fce4ec,stroke:#c2185b

    class START start
    class LOAD,SEARCH,ASSEMBLE,VALIDATE,DEDUP,CLASSIFY_CASE,ADD_TAG,SAVE_CASE process
    class GEN,RETRY ai
    class APPROVE_REVIEW,DETAIL decision
    class START_REVIEW,ASSIGN,PUSH,WAIT,APPROVED_STATUS,CLOSE_CASE,BATCH_APPROVE review
    class MODIFY,FEEDBACK_CASE,REJECT,SINGLE_FIX,BATCH_FIX endState
```

### 用例生成策略

| 策略 | 说明 | 适用场景 |
|------|------|----------|
| 等价类划分 | 输入域划分为有效/无效等价类 | 数据输入验证 |
| 边界值分析 | 测试边界及邻域值 | 数值/字符长度校验 |
| 场景法 | 主成功场景 + 扩展场景 | 业务流程测试 |
| 正交实验法 | 多参数组合的约减覆盖 | 配置组合测试 |
| 错误推测法 | 基于历史失败模式推测 | 回归测试补充 |

### 评审状态机

```
                    ┌──────────────┐
                    │   待评审     │
                    └──────┬───────┘
                           │
                ┌──────────┼──────────┐
                ▼          ▼          ▼
           ┌────────┐ ┌────────┐ ┌────────┐
           │ 已确认  │ │需修改  │ │ 已拒绝  │
           └────┬───┘ └────┬───┘ └────────┘
                │          │
                ▼          ▼
          进入脚本生成  退回重生成
```

---

## 4. 测试执行流程

```mermaid
flowchart TB
    START(["📋 触发执行<br/>手动 / 定时 / CI 触发"])

    START --> INIT["初始化执行任务<br/>Dataverse ExecutionRun"]
    INIT --> LOAD_SCRIPTS["加载脚本列表<br/>从 Dataverse TestScript"]

    LOAD_SCRIPTS --> SELECT_ENGINE{"引擎选择"}
    SELECT_ENGINE -->|有效| PLAYWRIGHT["Playwright 执行器"]
    SELECT_ENGINE -->|兼容| SELENIUM["Selenium 执行器"]

    PLAYWRIGHT --> ANALYZE["动态分析页面结构<br/>更新元素定位器"]
    ANALYZE --> SETUP["设置测试环境<br/>数据准备"]

    SELENIUM --> SETUP

    SETUP --> DISPATCH["分发用例到执行器池<br/>5 个并发槽位 / 执行器"]

    DISPATCH --> LOOP{"还有未执行用例?"}

    LOOP -->|是| EXEC["执行单个用例<br/>Playwright / Selenium"]
    EXEC --> RECORD["记录执行结果<br/>(通过/失败/阻塞/跳过)"]
    RECORD --> SCREENSHOT["截图 + DOM 快照<br/>上传 Blob Storage"]
    RECORD --> LOG["收集浏览器日志 / 控制台输出<br/>上传 Blob Storage"]
    RECORD --> METRICS["采集性能指标<br/>(加载时间/响应时间/QPS)"]

    METRICS --> STREAM["流式写入 Azure SQL<br/>ExecutionResult"]

    STREAM --> CHECK_HEALTH{"执行器健康检查"}
    CHECK_HEALTH -->|正常| LOOP
    CHECK_HEALTH -->|异常| RETRY_EXEC["失败重试<br/>最多 3 次"] --> EXEC

    LOOP -->|否| AGGREGATE["聚合执行结果<br/>SQL 汇总统计"]
    AGGREGATE --> SUMMARY["生成执行摘要<br/>通过率 / 失败率 / 耗时"]
    SUMMARY --> UPDATE_RUN["更新执行任务状态<br/>Dataverse ExecutionRun"]
    UPDATE_RUN --> POST_PROC["触发后续流程<br/>失败分析 / 报告生成"]

    subgraph "执行器内部详细流程"
        PLAYWRIGHT_DETAIL["Playwright<br/>- Chromium/Firefox/WebKit<br/>- 自动等待<br/>- 截图对比<br/>- 性能采集<br/>- 网络拦截"]
        SELENIUM_DETAIL["Selenium<br/>- 兼容旧脚本<br/>- 截图<br/>- WebDriver 管理"]
    end

    PLAYWRIGHT --- PLAYWRIGHT_DETAIL
    SELENIUM --- SELENIUM_DETAIL

    classDef start fill:#c8e6c9,stroke:#2e7d32
    classDef process fill:#e3f2fd,stroke:#1565c0
    classDef decision fill:#fff9c4,stroke:#f9a825
    classDef engine fill:#ffccbc,stroke:#d84315
    classDef detail fill:#e1bee7,stroke:#6a1b9a

    class START start
    class INIT,LOAD_SCRIPTS,ANALYZE,SETUP,DISPATCH,RECORD,SCREENSHOT,LOG,METRICS,STREAM,AGGREGATE,SUMMARY,UPDATE_RUN,POST_PROC process
    class SELECT_ENGINE,LOOP,CHECK_HEALTH decision
    class PLAYWRIGHT,SELENIUM,EXEC,RETRY_EXEC engine
    class PLAYWRIGHT_DETAIL,SELENIUM_DETAIL detail
```

### 执行并行度配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 每个执行器并发槽位 | 5 | 每个 Playwright 容器同时执行 5 个用例 |
| 执行器池大小 | 10 | 最大运行容器数，水平自动扩展 |
| 最大总并发 | 50 | 系统级上限 |
| 单个用例超时 | 180s | 超时后标记为阻塞 |
| 失败重试次数 | 3 | 失败后自动重试 |
| 重试间隔 | 30s | 指数退避 |

### 执行结果状态码

| 状态 | 含义 | 后续动作 |
|------|------|----------|
| PASSED | 通过 | 无 |
| FAILED | 断言失败 | 触发失败分析 |
| BROKEN | 脚本/环境异常 | 触发失败分析 + 标记脚本问题 |
| SKIPPED | 跳过 | 无 |
| BLOCKED | 超时 | 触发失败分析 |
| PENDING | 等待执行 | 继续等待 |

---

## 5. 失败分析流程

```mermaid
flowchart TB
    START(["🚨 检测到失败用例"]) --> COLLECT["收集失败上下文"]

    COLLECT --> GET_LOG["获取执行日志<br/>从 Blob Storage"]
    COLLECT --> GET_SCREENSHOT["获取失败截图<br/>从 Blob Storage"]
    COLLECT --> GET_DOM["获取 DOM 快照<br/>从 Blob Storage"]
    COLLECT --> GET_VIDEO["获取录屏回放<br/>(可选)"]

    GET_LOG --> CLASSIFY_L1["L1 分类<br/>GPT-4o-mini<br/>快速判断类型"]

    CLASSIFY_L1 --> TYPE{"失败类型"}

    TYPE -->|断言失败| L2_ASSERT["L2 断言分析<br/>期望 vs 实际值对比<br/>截图 OCR"]
    TYPE -->|脚本异常| L2_SCRIPT["L2 脚本分析<br/>定位器失效/元素变更<br/>DOM 差异比对"]
    TYPE -->|环境异常| L2_ENV["L2 环境分析<br/>HTTP 状态码 / 网络状态<br/>服务可用性检查"]
    TYPE -->|超时| L2_TIMEOUT["L2 超时分析<br/>哪个步骤超时<br/>性能瓶颈判断"]

    L2_ASSERT --> ROOT_CAUSE["GPT-4o 根因分析<br/>全上下文输入"]
    L2_SCRIPT --> ROOT_CAUSE
    L2_ENV --> ROOT_CAUSE
    L2_TIMEOUT --> ROOT_CAUSE

    ROOT_CAUSE --> SEARCH_HISTORY["检索历史失败模式<br/>Azure AI Search"]
    SEARCH_HISTORY --> MATCH{"匹配已知模式?"}

    MATCH -->|是| KNOWN["已知缺陷<br/>引用历史 Bug ID"]
    MATCH -->|否| NEW["新缺陷<br/>产生新失败模式"]

    NEW --> CLASSIFY_LABEL["归类 & 打标签<br/>(组件/模块/严重程度)"]
    CLASSIFY_LABEL --> STORE_PATTERN["写入 AI Search<br/>failure-patterns 索引"]
    KNOWN --> CREATE_BUG{"自动创建 Bug?"}

    STORE_PATTERN --> CREATE_BUG
    CREATE_BUG -->|产品 Bug| DEVOPS["创建 Azure DevOps Bug<br/>附完整上下文"]
    CREATE_BUG -->|脚本问题| AUTO_FIX["自动修复脚本<br/>AI 调整定位器"]
    CREATE_BUG -->|环境问题| TEAMS_NOTIFY["Teams 通知运维<br/>附分析报告"]

    AUTO_FIX --> RE_EXEC["提交重新执行"]
    DEVOPS --> ASSIGN_DEV["分配开发人员"]
    TEAMS_NOTIFY --> RESOLVE_ENV["等待运维处理"]

    subgraph "失败上下文结构"
        CTX["{
          caseId: 'TC-001',
          script: 'login.spec.ts',
          error: expect(received).toBe(expected),
          screenshot: blob.url,
          domSnapshot: html.string,
          log: browser.log,
          network: failed.request,
          timestamp: 2026-08-18T10:30:00Z
        }"]
    end

    COLLECT --- CTX

    classDef start fill:#c8e6c9,stroke:#2e7d32
    classDef collect fill:#fff3e0,stroke:#e65100
    classDef classify fill:#e3f2fd,stroke:#1565c0
    classDef analysis fill:#e1bee7,stroke:#6a1b9a
    classDef decision fill:#fff9c4,stroke:#f9a825
    classDef action fill:#ffccbc,stroke:#d84315

    class START start
    class COLLECT,GET_LOG,GET_SCREENSHOT,GET_DOM,GET_VIDEO collect
    class CLASSIFY_L1 classify
    class L2_ASSERT,L2_SCRIPT,L2_ENV,L2_TIMEOUT,ROOT_CAUSE,SEARCH_HISTORY,CLASSIFY_LABEL analysis
    class TYPE,MATCH,CREATE_BUG decision
    class KNOWN,NEW,STORE_PATTERN,DEVOPS,AUTO_FIX,TEAMS_NOTIFY,RE_EXEC,ASSIGN_DEV,RESOLVE_ENV action
```

### 失败分类体系

```
失败
├── 断言失败 (Assertion Failure)
│   ├── 文本不匹配——页面显示与预期不符
│   ├── 元素状态不符——不可点击/不可见/不存在
│   └── 数据比对失败——列表/表格数据不一致
├── 脚本异常 (Script Error)
│   ├── 定位器失效——元素选择器不再匹配
│   ├── Frame/窗口切换异常——无法定位目标窗口
│   ├── 等待超时——元素始终未达到期望状态
│   └── 依赖未就绪——前置数据/状态未准备好
├── 环境异常 (Environment Error)
│   ├── HTTP 500/502/503——服务端错误
│   ├── 网络连接超时——请求无响应
│   ├── 认证过期——Token/Session 失效
│   └── 数据库异常——数据不一致/锁冲突
└── 超时 (Timeout)
    ├── 页面加载超时——整体渲染超过阈值
    ├── API 响应超时——后端接口慢
    └── 操作超时——单个操作耗时过长
```

---

## 6. 报告生成流程

```mermaid
flowchart TB
    START(["📊 触发报告生成<br/>执行完成 / 手动 / 定时"])

    START --> INIT_REPORT["初始化报告记录<br/>Dataverse TestReport"]

    INIT_REPORT --> AGG["数据聚合"]

    AGG --> AGG_RESULT["聚合执行结果<br/>Azure SQL → 汇总统计"]
    AGG --> AGG_FAILURE["聚合失败分析<br/>AI Search → 每项失败详情"]
    AGG --> AGG_METRICS["聚合性能指标<br/>Azure SQL → 响应时间分布"]
    AGG --> AGG_HISTORY["聚合历史趋势<br/>Azure SQL → 历史对比"]

    AGG_RESULT --> BUILD_SUMMARY["生成报告摘要<br/>GPT-4o 分析总结"]

    BUILD_SUMMARY --> INCLUDE["报告包含内容"]
    INCLUDE --> S1["📈 执行概览：通过率/失败率/阻塞率"]
    INCLUDE --> S2["🔴 失败详情：每项失败 + 根因 + 建议"]
    INCLUDE --> S3["📊 性能分析：响应时间/加载时间/QPS"]
    INCLUDE --> S4["📉 趋势图：历史通过率/失败率变化"]
    INCLUDE --> S5["📋 用例覆盖率：模块/功能/优先级"]
    INCLUDE --> S6["💡 AI 建议：高风险模块/改进方向"]

    S1 --> FILL["填充报告模板<br/>HTML / PDF 模板"]
    S2 --> FILL
    S3 --> FILL
    S4 --> FILL
    S5 --> FILL
    S6 --> FILL

    FILL --> GENERATE["生成报告文件"]
    GENERATE --> RENDER_PDF["PDF 版本<br/>适合打印/归档"]
    GENERATE --> RENDER_HTML["HTML 版本<br/>适合在线查看"]
    GENERATE --> RENDER_JSON["JSON 版本<br/>适合系统对接"]

    RENDER_PDF --> UPLOAD["上传 Blob Storage<br/>reports/{runId}/"]
    RENDER_HTML --> UPLOAD
    RENDER_JSON --> UPLOAD

    UPLOAD --> GEN_LINK["生成分享链接<br/>SAS Token 临时链接"]
    GEN_LINK --> DISTRIBUTE["分发报告"]

    DISTRIBUTE --> DIST_TEAMS["Teams 推送<br/>Adaptive Card + 摘要"]
    DISTRIBUTE --> DIST_MAIL["邮件发送<br/>Outlook + PDF 附件"]
    DISTRIBUTE --> DIST_PORTAL["用户门户查看<br/>Power Pages"]
    DISTRIBUTE --> DIST_DEVOPS["附到 DevOps<br/>Test Run 结果"]

    DIST_TEAMS --> DONE["✅ 完成"]
    DIST_MAIL --> DONE
    DIST_PORTAL --> DONE
    DIST_DEVOPS --> DONE

    classDef start fill:#c8e6c9,stroke:#2e7d32
    classDef process fill:#e3f2fd,stroke:#1565c0
    classDef agg fill:#e8f5e9,stroke:#2e7d32
    classDef content fill:#f3e5f5,stroke:#6a1b9a
    classDef gen fill:#fff3e0,stroke:#e65100
    classDef dist fill:#e0f7fa,stroke:#00838f

    class START start
    class INIT_REPORT,AGG,BUILD_SUMMARY,INCLUDE,FILL process
    class AGG_RESULT,AGG_FAILURE,AGG_METRICS,AGG_HISTORY agg
    class S1,S2,S3,S4,S5,S6 content
    class GENERATE,RENDER_PDF,RENDER_HTML,RENDER_JSON,UPLOAD,GEN_LINK gen
    class DISTRIBUTE,DIST_TEAMS,DIST_MAIL,DIST_PORTAL,DIST_DEVOPS,DONE dist
```

### 报告模板结构

```
测试报告：{项目名称} - {执行时间}
├── 1. 执行概览
│   ├── 总用例数：X | 通过：Y | 失败：Z | 跳过：W
│   ├── 通过率：Y/X % | 执行耗时：T min
│   └── 环境信息：浏览器 / 版本 / 操作系统
├── 2. 失败详情
│   ├── TC001 - 用户名密码登录
│   │   ├── 失败类型：断言失败
│   │   ├── 根因：页面“登录成功”文案变更（期望→实际）
│   │   └── AI 建议：更新断言文本
│   └── TC002 - ...
├── 3. 性能分析
│   ├── 平均加载时间：2.3s
│   ├── P95 响应时间：4.1s
│   └── TOP 3 慢加载页面
├── 4. 趋势对比
│   ├── 本次通过率：85%
│   ├── 上次通过率：90%
│   └── 偏差：-5% ↑ 需关注
├── 5. 覆盖率分析
│   ├── 模块覆盖率：登录(100%) / 搜索(85%) / ...
│   └── 优先级分布：P0(100%) / P1(90%) / P2(60%)
└── 6. AI 分析结论
    └── 风险提示 & 改进建议
```

# 系统架构图

> AI-Powered Intelligent Testing Platform 的架构可视化

---

## 1. 系统整体架构图（C4 Context 图）

```mermaid
C4Context
    title System Context diagram for AI-Powered Intelligent Testing Platform

    Person(tester, "测试工程师", "核心用户：管理用例、执行测试、查看报告")
    Person(ba, "业务分析师", "上传需求、参与评审")
    Person(manager, "测试经理", "查看报告、管理团队")
    Person(dev, "开发工程师", "查看失败分析结果")
    Person(external, "外部客户", "通过门户提交需求和查看报告")

    System_Boundary(platform, "AI 测试平台") {
        System(powerapps, "Power Apps UI", "内部用户工作台")
        System(powerpages, "Power Pages", "外部客户门户")
        System(copilot, "Copilot Studio", "AI 对话助手")
        System(automate, "Power Automate", "流程编排引擎")
        System(functions, "Azure Functions", "AI 编排与业务逻辑")
        System(openai, "Azure OpenAI", "LLM 推理服务")
        System(search, "Azure AI Search", "向量语义搜索")
        System(playwright, "Playwright Engine", "UI 自动化执行")
        System(selenium, "Selenium Engine", "遗留脚本兼容")
    }

    System_Ext(devops, "Azure DevOps", "工作项与 CI 集成")
    System_Ext(jira, "Jira（可选）", "问题跟踪")
    System_Ext(teams, "Microsoft Teams", "通知与协作")
    System_Ext(outlook, "Outlook", "邮件通知")

    Rel(tester, powerapps, "使用", "HTTPS")
    Rel(ba, powerapps, "使用", "HTTPS")
    Rel(manager, powerapps, "使用", "HTTPS")
    Rel(dev, powerapps, "使用", "HTTPS")
    Rel(external, powerpages, "使用", "HTTPS")
    Rel(tester, copilot, "对话交互", "HTTPS + WebSocket")

    Rel(powerapps, automate, "触发流程", "Power Platform 连接器")
    Rel(powerpages, automate, "触发流程", "Power Platform 连接器")
    Rel(copilot, functions, "调用 API", "HTTPS + 托管标识")
    Rel(automate, functions, "调用 Azure Function", "托管标识")
    Rel(functions, openai, "LLM 推理请求", "Private Endpoint")
    Rel(functions, search, "向量检索", "Private Endpoint")
    Rel(functions, playwright, "触发执行", "内部 API")
    Rel(functions, selenium, "触发执行", "内部 API")
    Rel(automate, devops, "同步工作项", "REST API + Service Hook")
    Rel(automate, jira, "同步问题", "REST API（可选）")
    Rel(automate, teams, "推送通知", "Teams Connector")
    Rel(automate, outlook, "发送邮件", "Outlook Connector")
```

---

## 2. 分层架构图

```mermaid
graph TB
    subgraph "表现层 (Presentation)"
        PA["Power Apps<br/>内部工作台"]
        PP["Power Pages<br/>外部门户"]
        CS["Copilot Studio<br/>AI 助手面板"]
        TA["Teams App<br/>内嵌应用"]
    end

    subgraph "入口层 (Gateway)"
        APIM["Azure API Management<br/>API 网关"]
        PE["Power Platform<br/>内部路由"]
    end

    subgraph "应用层 (Application)"
        FW["Power Automate Flows<br/>流程编排 & 审批"]
        FN["Azure Functions<br/>AI 编排 & 业务逻辑"]
    end

    subgraph "服务层 (Services)"
        subgraph "AI 服务"
            AO["Azure OpenAI<br/>GPT-4o"]
            AS["Azure AI Search<br/>向量 + 语义搜索"]
            ACS["Azure Cognitive Search<br/>(过渡期兼容)"]
        end
        subgraph "执行引擎"
            PL["Playwright Engine<br/>主执行器"]
            SE["Selenium Engine<br/>兼容执行器"]
        end
        subgraph "业务服务"
            AH["Auth Service<br/>认证授权"]
            NS["Notification Service<br/>消息推送"]
        end
    end

    subgraph "数据层 (Data)"
        DV["Dataverse<br/>业务主数据<br/>(需求/用例/评审/脚本)"]
        SQL["Azure SQL<br/>执行结果 & 性能数据"]
        SP["SharePoint<br/>需求文档附件"]
        BS["Azure Blob Storage<br/>截图/日志/报告/AI缓存"]
    end

    subgraph "集成层 (Integration)"
        ADO["Azure DevOps"]
        JIRA["Jira"]
        TEAMS["Teams"]
        OUL["Outlook"]
    end

    PA --> PE
    PP --> PE
    CS --> APIM
    TA --> PE

    PE --> FW
    APIM --> FN

    FW --> FN
    FN --> AO
    FN --> AS
    FN --> ACS
    FN --> PL
    FN --> SE

    FN --> DV
    FN --> SQL
    FN --> BS
    FN --> SP
    FW --> DV

    FW --> ADO
    FW --> JIRA
    FW --> TEAMS
    FW --> OUL

    PL --> DV
    PL --> SQL
    PL --> BS
    SE --> SQL
    SE --> BS

    classDef presentation fill:#e1f5fe,stroke:#0288d1,color:#000
    classDef gateway fill:#fff3e0,stroke:#f57c00,color:#000
    classDef application fill:#e8f5e9,stroke:#388e3c,color:#000
    classDef services fill:#fce4ec,stroke:#c2185b,color:#000
    classDef data fill:#f3e5f5,stroke:#7b1fa2,color:#000
    classDef integration fill:#fff8e1,stroke:#f9a825,color:#000

    class PA,PP,CS,TA presentation
    class APIM,PE gateway
    class FW,FN application
    class AO,AS,ACS,PL,SE,AH,NS services
    class DV,SQL,SP,BS data
    class ADO,JIRA,TEAMS,OUL integration
```

---

## 3. 部署架构图（Azure Topology）

```mermaid
graph TB
    subgraph "Azure AD / Entra ID"
        AAD["身份认证<br/>Managed Identity + Service Principal"]
    end

    subgraph "Customer VNet - 10.x.0.0/16"
        subgraph "Integration Subnet - 10.x.1.0/24"
            APIM["Azure API Management<br/>Premium Tier"]
            FW2["Azure Firewall"]
        end

        subgraph "Compute Subnet - 10.x.2.0/24"
            APPS["App Service Plan<br/>(Functions Hosting)"]
            FN1["Azure Functions<br/>AI Orchestrator"]
            FN2["Azure Functions<br/>Script Generator"]
            FN3["Azure Functions<br/>Result Processor"]
        end

        subgraph "Executor Subnet - 10.x.3.0/24"
            ACI["Azure Container Instances<br/>Playwright Pools"]
            ACI2["Container Group<br/>Selenium Nodes"]
        end

        subgraph "Data Subnet - 10.x.4.0/24"
            PE_SQL["Private Endpoint<br/>Azure SQL"]
            PE_BLOB["Private Endpoint<br/>Blob Storage"]
        end
    end

    subgraph "Power Platform Boundary"
        PA["Power Apps"]
        PP["Power Pages"]
        CS["Copilot Studio"]
        FW["Power Automate"]
        DV["Dataverse"]
        SP["SharePoint"]
    end

    subgraph "SaaS Services"
        AO["Azure OpenAI<br/>GPT-4o"]
        AIS["Azure AI Search"]
        ADO["Azure DevOps"]
        TEAMS["Teams"]
    end

    AAD --> PA
    AAD --> APIM

    PA --> FW
    PP --> FW
    CS --> APIM
    FW --> APIM

    APIM --> FN1
    APIM --> FN2
    APIM --> FN3

    FN1 --> AO
    FN1 --> AIS
    FN2 --> ACI
    FN2 --> ACI2
    FN3 --> ACI
    FN3 --> ACI2

    FN1 --> PE_SQL
    FN1 --> PE_BLOB
    FN2 --> PE_SQL
    FN2 --> PE_BLOB
    FN3 --> PE_SQL
    FN3 --> PE_BLOB

    FW --> DV
    FW --> SP
    FW --> ADO
    FW --> TEAMS

    ACI --- PE_BLOB
    ACI2 --- PE_SQL

    classDef vnet fill:#e3f2fd,stroke:#1565c0,color:#000
    classDef subnet fill:#e8eaf6,stroke:#283593,color:#000
    classDef pe fill:#f3e5f5,stroke:#6a1b9a,color:#000
    classDef power fill:#e8f5e9,stroke:#2e7d32,color:#000
    classDef saas fill:#fff3e0,stroke:#e65100,color:#000

    class FW2,APIM subnet
    class FN1,FN2,FN3 subnet
    class ACI,ACI2 subnet
    class PE_SQL,PE_BLOB pe
    class PA,PP,CS,FW,DV,SP power
    class AO,AIS,ADO,TEAMS saas
```

---

## 4. 组件间交互关系图

```mermaid
graph LR
    subgraph 用户层
        U["👤 用户"]
    end

    subgraph 交互界面
        PA["Power Apps"]
        PP["Power Pages"]
        COP["Copilot Studio"]
    end

    subgraph 协调中枢
        FAM["Power Automate<br/>流程管理器"]
        FA["Power Automate<br/>审批流"]
        FAN["Power Automate<br/>通知流"]
    end

    subgraph AI编排
        API["Azure Functions<br/>API 入口"]
        ORC["Azure Functions<br/>AI 编排器"]
        GEN["Azure Functions<br/>脚本生成器"]
    end

    subgraph AI服务
        LLM["Azure OpenAI<br/>GPT-4o"]
        SRCH["Azure AI Search"]
    end

    subgraph 执行引擎
        PL["Playwright Pool"]
        SL["Selenium Pool"]
    end

    subgraph 持久化
        DV["Dataverse"]
        SQL["Azure SQL"]
        BL["Blob Storage"]
        SHR["SharePoint"]
    end

    U -->|操作| PA
    U -->|操作| PP
    U -->|对话| COP
    PA -->|触发| FAM
    PP -->|触发| FAM
    COP -->|调用| API
    FAM -->|协调| FA
    FAM -->|协调| FAN
    FAM -->|调用| API
    API -->|编排| ORC
    ORC -->|生成请求| GEN
    ORC -->|LLM推理| LLM
    ORC -->|知识检索| SRCH
    GEN -->|生成脚本| PL
    GEN -->|生成脚本| SL
    PL -->|写结果| SQL
    PL -->|存截图| BL
    SL -->|写结果| SQL
    SL -->|存截图| BL
    ORC -->|持久化| DV
    ORC -->|写结果| SQL
    ORC -->|存缓存| BL
    FA -->|更新状态| DV
    FAN -->|通知| U

    classDef user fill:#fff9c4,stroke:#f9a825
    classDef ui fill:#bbdefb,stroke:#1565c0
    classDef flow fill:#c8e6c9,stroke:#2e7d32
    classDef func fill:#ffccbc,stroke:#d84315
    classDef ai fill:#e1bee7,stroke:#6a1b9a
    classDef engine fill:#ffe0b2,stroke:#e65100
    classDef data fill:#d7ccc8,stroke:#4e342e

    class U user
    class PA,PP,COP ui
    class FAM,FA,FAN flow
    class API,ORC,GEN func
    class LLM,SRCH ai
    class PL,SL engine
    class DV,SQL,BL,SHR data
```

### 端口与协议说明

| 组件 | 协议 | 端口 | 认证方式 |
|------|------|------|----------|
| Power Apps → Power Automate | HTTPS | 443 | Entra ID 委托认证 |
| Power Automate → Azure Functions | HTTPS | 443 | 托管标识 (Managed Identity) |
| Azure Functions → Azure OpenAI | HTTPS | 443 | 托管标识 / API Key (Private Endpoint) |
| Azure Functions → Azure AI Search | HTTPS | 443 | API Key (Private Endpoint) |
| Azure Functions → Playwright | HTTP (内部) | 5000-5010 | Function Key |
| Playwright → Blob Storage | HTTPS | 443 | SAS Token |
| Power Automate → Teams | HTTPS | 443 | Teams Connector (OAuth) |
| Copilot Studio → Azure Functions | HTTPS | 443 | 托管标识 |
| Power Automate → Dataverse | HTTPS | 443 | Power Platform 内部路由 |
| Playwright → Azure SQL | TCP | 1433 | Private Endpoint + Entra ID |

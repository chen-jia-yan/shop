# 数据流图

> AI-Powered Intelligent Testing Platform 的数据流转可视化

---

## 1. 主数据流图 — 从需求到报告的完整数据流转

```mermaid
flowchart TB
    subgraph 输入
        REQ["📄 需求文档<br/>(PDF/Word/Markdown)"]
    end

    subgraph "阶段一：需求解析"
        A1["上传到 SharePoint"]
        A2["Azure Functions 读取文档"]
        A3["Azure OpenAI 解析需求<br/>提取：功能点、业务规则、边界条件"]
        A4["写入 Dataverse<br/>Requirement 实体"]
    end

    subgraph "阶段二：用例生成"
        B1["查询 AI Search<br/>相似历史用例"]
        B2["组装 Prompt<br/>(需求 + 历史用例 + 模板)"]
        B3["Azure OpenAI 生成测试用例<br/>Function Calling 结构化输出"]
        B4["写入 Dataverse<br/>TestCase 实体<br/>状态：待评审"]
    end

    subgraph "阶段三：评审流程"
        C1["Power Automate 发起评审"]
        C2["Teams 推送评审审批卡"]
        C3["评审员审批/修改/拒绝"]
        C4["反馈写入 Dataverse<br/>ReviewRecord 实体"]
        C5{"评审通过?"}
        C6["根据反馈调整用例<br/>重新生成"]
    end

    subgraph "阶段四：自动化脚本生成"
        D1["已确认用例 → Functions"]
        D2["Azure OpenAI 生成 Playwright 脚本"]
        D3["写入 Dataverse<br/>TestScript 实体"]
        D4["脚本预检<br/>(语法检查 + 静态分析)"]
    end

    subgraph "阶段五：执行"
        E1["Power Automate 调度执行"]
        E2["Playwright 执行器运行"]
        E3["Selenium 执行器运行<br/>(兼容脚本)"]
        E4["实时结果写入<br/>Azure SQL"]
        E5["截图/QPS/日志<br/>写入 Blob Storage"]
    end

    subgraph "阶段六：失败分析"
        F1["检测失败用例"]
        F2["收集失败上下文<br/>（截图 + 日志 + DOM 快照）"]
        F3["Azure OpenAI 分析失败原因"]
        F4["归类失败模式<br/>写入 AI Search 索引"]
        F5["Azure DevOps / Jira 创建 Bug"]
        F6["Teams 通知相关责任人"]
    end

    subgraph "阶段七：报告生成"
        G1["聚合执行结果<br/>从 SQL + Dataverse + Blob"]
        G2["Azure OpenAI 生成<br/>报告摘要 & 分析结论"]
        G3["填充报告模板"]
        G4["写入 Blob Storage<br/>Reports 目录"]
        G5["Power Automate 分发报告<br/>Teams + Outlook"]
    end

    REQ --> A1
    A1 --> A2 --> A3 --> A4
    A4 --> B1 --> B2 --> B3 --> B4
    B4 --> C1 --> C2 --> C3 --> C4 --> C5
    C5 -->|通过| D1
    C5 -->|拒绝| C6 --> B3
    D1 --> D2 --> D3 --> D4
    D4 --> E1 --> E2
    E1 --> E3
    E2 --> E4
    E2 --> E5
    E3 --> E4
    E3 --> E5
    E4 --> F1 --> F2 --> F3 --> F4
    F3 --> F5
    F4 --> F6
    E4 --> G1
    E5 --> G1
    F4 --> G1
    G1 --> G2 --> G3 --> G4 --> G5

    classDef input fill:#fff9c4,stroke:#f9a825
    classDef parse fill:#e3f2fd,stroke:#1565c0
    classDef generate fill:#e8f5e9,stroke:#2e7d32
    classDef review fill:#fff3e0,stroke:#e65100
    classDef script fill:#fce4ec,stroke:#c2185b
    classDef exec fill:#f3e5f5,stroke:#6a1b9a
    classDef analyze fill:#ffccbc,stroke:#d84315
    classDef report fill:#e0f7fa,stroke:#00838f
    classDef decision fill:#fbe9e7,stroke:#bf360c

    class REQ input
    class A1,A2,A3,A4 parse
    class B1,B2,B3,B4 generate
    class C1,C2,C3,C4,C6 review
    class D1,D2,D3,D4 script
    class E1,E2,E3,E4,E5 exec
    class F1,F2,F3,F4,F5,F6 analyze
    class G1,G2,G3,G4,G5 report
    class C5 decision
```

---

## 2. AI 处理流水线 — 数据如何在 AI 服务间流转

```mermaid
graph LR
    subgraph "输入层"
        DOC["需求文档"]
        HIS["历史用例<br/>Dataverse"]
        FAIL["历史失败模式<br/>AI Search"]
    end

    subgraph "RAG 检索"
        EMB["Embedding 模型<br/>text-embedding-3-large"]
        VS["向量索引<br/>Azure AI Search"]
        HYBRID["混合检索<br/>向量 + 关键词"]
    end

    subgraph "Prompt 组装"
        TP["模板引擎<br/>系统 Prompt"]
        CTX["上下文拼接<br/>需求 + 检索结果"]
        SCH["Function Calling Schema<br/>结构化输出格式"]
    end

    subgraph "LLM 推理"
        G40["GPT-4o<br/>主推理模型"]
        G4M["GPT-4o-mini<br/>简单任务<br/>(分类/提取)"]
        FNC["Function Calling<br/>结构化输出"]
    end

    subgraph "后处理"
        VAL["结果校验<br/>JSON Schema 验证"]
        DEDUP["去重<br/>对比已有用例"]
        FMT["格式化<br/>写入数据模型"]
    end

    subgraph "存储"
        DV2["Dataverse"]
        SQL2["Azure SQL"]
        BS2["Blob Storage<br/>AI Cache"]
        IDX["AI Search 索引<br/>更新"]
    end

    DOC --> EMB
    HIS --> EMB
    FAIL --> VS

    EMB --> VS
    VS --> HYBRID
    DOC --> TP
    HYBRID --> CTX
    TP --> CTX
    CTX --> G40
    CTX --> G4M
    SCH --> G40
    SCH --> G4M
    G40 --> VAL
    G4M --> VAL
    VAL --> DEDUP
    DEDUP --> FMT
    FMT --> DV2
    FMT --> SQL2
    FMT --> BS2
    FMT --> IDX

    classDef input fill:#fff9c4,stroke:#f9a825
    classDef rag fill:#e3f2fd,stroke:#1565c0
    classDef prompt fill:#e8f5e9,stroke:#2e7d32
    classDef llm fill:#fce4ec,stroke:#c2185b
    classDef post fill:#f3e5f5,stroke:#6a1b9a
    classDef store fill:#d7ccc8,stroke:#4e342e

    class DOC,HIS,FAIL input
    class EMB,VS,HYBRID rag
    class TP,CTX,SCH prompt
    class G40,G4M,FNC llm
    class VAL,DEDUP,FMT post
    class DV2,SQL2,BS2,IDX store
```

### AI 调用场景详细表

| 场景 | 模型 | 输入 | 输出 | 最大 Token | 超时 |
|------|------|------|------|-----------|------|
| 需求解析 | GPT-4o | 需求文档全文 | 结构化功能点列表 | 16K | 60s |
| 用例生成 | GPT-4o | 需求片段 + 历史用例 | 测试用例列表 (JSON) | 32K | 120s |
| 脚本生成 | GPT-4o | 用例描述 + 页面结构 | Playwright 脚本代码 | 32K | 120s |
| 失败分类 | GPT-4o-mini | 错误日志 + 截图描述 | 失败分类标签 | 4K | 30s |
| 失败分析 | GPT-4o | 全上下文 + 历史模式 | 根因分析 + 修复建议 | 16K | 60s |
| 报告摘要 | GPT-4o | 聚合结果数据 | 报告摘要文本 | 8K | 45s |

---

## 3. 文件存储流 — Blob Storage 中的文档流转

```mermaid
flowchart TB
    subgraph "Blob Storage 容器"
        CON1["📁 requirements<br/>需求文档原件"]
        CON2["📁 screenshots<br/>执行截图"]
        CON3["📁 logs<br/>执行日志"]
        CON4["📁 reports<br/>测试报告"]
        CON5["📁 ai-cache<br/>AI 推理缓存"]
        CON6["📁 scripts<br/>生成脚本备份"]
    end

    subgraph "路径规划"
        P1["requirements/{reqId}/{filename}"]
        P2["screenshots/{runId}/{caseId}/{ts}.png"]
        P3["logs/{runId}/{caseId}/execution.log"]
        P4["reports/{runId}/summary-report.pdf"]
        P5["reports/{runId}/detail-report.json"]
        P6["ai-cache/{sessionId}/{step}.json"]
        P7["scripts/{caseId}/playwright/{scriptName}.ts"]
        P8["scripts/{caseId}/selenium/{scriptName}.java"]
    end

    subgraph "生命周期"
        L1["需求文档：长期保留<br/>(90 天后归档冷存储)"]
        L2["截图日志：保留 30 天<br/>过期自动删除"]
        L3["报告：长期保留<br/>按版本归档"]
        L4["AI 缓存：保留 24 小时<br/>TTL 过期清理"]
        L5["脚本备份：长期保留<br/>关联用例生命周期"]
    end

    subgraph "访问管控"
        AC1["Power Apps / Pages<br/>→ SAS Token 只读"]
        AC2["Azure Functions<br/>→ 托管标识 读写"]
        AC3["Playwright 执行器<br/>→ SAS Token 写入"]
        AC4["报告分发<br/>→ SAS Token 临时链接"]
    end

    CON1 --> P1
    CON2 --> P2
    CON3 --> P3
    CON4 --> P4
    CON4 --> P5
    CON5 --> P6
    CON6 --> P7
    CON6 --> P8

    P1 --> L1
    P2 --> L2
    P3 --> L2
    P4 --> L3
    P5 --> L3
    P6 --> L4
    P7 --> L5
    P8 --> L5

    L1 -.-> AC1
    L1 -.-> AC2
    L2 -.-> AC3
    L3 -.-> AC1
    L3 -.-> AC4
    L4 -.-> AC2

    classDef container fill:#e3f2fd,stroke:#1565c0,color:#000
    classDef path fill:#e8f5e9,stroke:#2e7d32,color:#000
    classDef life fill:#fff3e0,stroke:#e65100,color:#000
    classDef access fill:#fce4ec,stroke:#c2185b,color:#000

    class CON1,CON2,CON3,CON4,CON5,CON6 container
    class P1,P2,P3,P4,P5,P6,P7,P8 path
    class L1,L2,L3,L4,L5 life
    class AC1,AC2,AC3,AC4 access
```

### 存储容量预估

| 容器 | 单次执行量 | 月增量（1000次执行） | 年存储 |
|------|-----------|-------------------|--------|
| requirements | 1-10 MB / 需求 | 100 MB | 1.2 GB |
| screenshots | 5-20 MB / 执行 | 15 GB | 180 GB |
| logs | 0.5-5 MB / 执行 | 2.5 GB | 30 GB |
| reports | 0.5-2 MB / 报告 | 1.5 GB | 18 GB |
| ai-cache | 1-5 MB / 会话 | 3 GB | 36 GB（24h TTL）|
| scripts | 10-50 KB / 脚本 | 50 MB | 600 MB |

### 冷热分层策略

| 层级 | 适用数据 | 存储类型 |
|------|----------|----------|
| Hot | 截图（7 天内）、当前报告、AI 缓存 | Blob Storage Hot Tier |
| Cool | 截图（7-30 天）、历史报告 | Blob Storage Cool Tier |
| Cold | 需求原件（90 天后）、归档报告 | Blob Storage Archive Tier |

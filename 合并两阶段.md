# 任务:把批处理的"两阶段(先文字后 OCR)"简化为"单趟处理"

## 背景
当前批处理 worker 把每个文件分两阶段:先 text 阶段(只抽文字),再 full 阶段(OCR 图片),中间靠状态机(text_done / ocr_pending / ocr_processing)交接。这套交接带来了额外复杂度、同一文件被解析两遍、以及中间态卡住等问题。我要简化为:**每个文件一趟做完(文字 + OCR 一起),只有一个终态。**

## 先做(务必)
先不要改代码。先告诉我:你打算改哪些文件、每个文件改哪里、为什么、有什么风险。等我确认后再动手,小步进行、每步可回退。

## 要实现的改造
1. **状态机简化**为:`pending → processing → completed / failed / dead`。移除 `text_done`、`ocr_pending`、`ocr_processing` 这三个中间态及相关分支。
2. **process_one 一趟做完**:每个文件在一次调用里完成"抽文字 + OCR 图片"(即恒为 include_ocr=True),不再有 phase='text'/'full' 的区分,也不再返回 next_phase='ocr'。
3. **claim_pending 简化**:只领取 `pending` 和可重试的 `failed`(仍受 attempts / crash_count 上限约束),移除对 ocr_pending 的处理。
4. **入库只写一次**:一个文件完全处理好后,一次性写入 Azure AI Search,然后标记 `completed`。避免"先写一半"的中间状态。
5. **清理死代码**:移除 `_phase_for_stage`、phase 相关参数、ocr_pending/ocr_processing 相关的调度与重置逻辑。
6. **数据迁移**:数据库里现有处于 `text_done` / `ocr_pending` / `ocr_processing` 的记录,统一重置为 `pending`,让它们在新的单趟模型下重新处理(attempts、crash_count 归零)。

## 必须保持不变(别改坏)
- 重文件的独立子进程隔离(subprocess),且子进程解释器用 `sys.executable`(当前 .venv312)。
- 失败重试与崩溃计数(attempts / crash_count → dead)。
- 断点续跑、按文件哈希做的幂等/增量(重跑不清库、跳过没变的)。
- embedding 的全局限速与请求内批量。
- OCR 引擎每个 worker 只初始化一次并复用(不要每个文件重装)。
- 只用标准库 + 已安装包;不删除 PaddleOCR 模型缓存;Windows spawn,入口在 `if __name__ == "__main__"` 下。

## 验收标准
- 一个文件 = 一次 process_one(文字 + OCR)+ 一次入库 + 终态 completed;代码里不再出现 text_done / ocr_pending / ocr_processing。
- 现有中间态记录已被重置为 pending 并能重新跑通。
- 重文件隔离、重试、续跑、幂等、限速、OCR 复用全部照常工作。
- 改完给我一份改动清单:动了哪些文件、每处解决什么、怎么验证。

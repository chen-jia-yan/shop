## 解析链路

- `pptx_parser.py` 现在怎么处理 PPT:是只把整页/图片栅格化走 OCR,还是已经在遍历 shapes、读文本框?有没有碰过 connector?
   

- `excel_parser.py` 怎么处理 Excel:表格、图形、图片各走什么;你说的”行级 chunk + 单元格范围”具体怎么切。

- `pptx_ocr.py` 的触发和输出:OCR 出来的东西喂到哪、以什么结构存。

-  你后来加的那层**“结构化 + 语义抽取”**具体做了什么、输出什么结构?(连接线重建出的有向图要往这里接)

## 架构与数据结构

- `ingestor` → 解析 → 切块 → 灌索引 的调用链,大致顺序(方便我定”流程图建图”这一步插在哪)。

- chunk 的元数据 schema:`content_type` 有哪些取值?有没有地方能挂一个”流程图有向图”结构(比如存成 mermaid / 边列表)?

## 其他

- 重排现在换成了什么(哪个模型、是不是多语言 cross-encoder、候选集多大)——虽然跟流程图这块关系不大,但你提到了,一并确认能帮我对齐全局。

- 运行环境:`python-pptx` / `openpyxl` 版本,有没有 GPU/依赖限制。




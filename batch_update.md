# 任务:修复批处理泡测暴露的 4 类问题(按优先级分批)

## 背景
这是一个本地运行的日文保险知识库批处理系统:文件上传后入队,worker 多进程处理(读文本 → OCR 识别图片里的日文 → 切片 → embedding 向量化 → 写入 Azure AI Search),状态记录在本地 SQLite。系统跑了一整天没崩溃,但 79 个文件里有 20 个"可恢复的失败"。现在要把这些失败逐类修掉。

涉及的主要文件(以实际项目为准):
- `kb_admin/worker.py`:worker 主循环、claim 调度、两阶段、超时、pool 恢复
- `kb_admin/services.py`:上传、入队、ingest 入口
- `kb_admin/ingestion.py` / `kb_poc/ingestion/ingestor.py`:切片 `build_chunks_for_path` / `build_chunks_for_stage`、`write_chunk_batches`
- `kb_poc/retrieval/embedding.py`:embedding 请求
- `kb_poc/parsing/ppt_ocr.py`、`kb_poc/parsing/artifacts.py`:OCR、产物
- `kb_admin/state_store.py`:SQLite 状态机
- `kb_admin/kb_admin_actions.py`:知识管理动作、审批

## 硬性约束(务必遵守)
1. 只能用 Python 标准库 + 已安装的包,不能新增任何需要下载的依赖(公司网络会 blocked)。
2. 全部本地运行,数据不外发第三方;检索库只用 Azure AI Search。
3. 绝对不要删除或改动 PaddleOCR 的本地模型缓存(离线环境无法重新下载)。
4. Windows 环境,多进程用 spawn,入口必须有 `if __name__ == "__main__"`。
5. 改动要向后兼容,不破坏现有已跑通的流程;每处改动加清晰日志。

---

## 第①批(最优先,先干掉"假失败")

### 1. "no embedded image" 不应算失败
- 问题:文档没有内嵌图片是正常情况。文本阶段已入库,OCR 阶段发现无图可识别,现在被错误标成 `failed`。
- 要做:在 OCR/full 阶段,当检测到文件没有可 OCR 的图片时,视为该阶段正常完成,把状态标为 `completed`(而不是 `failed`),日志记为 info 级"no image, ocr stage skipped"。
- 验收:上传一个纯文字、无图的文档,最终状态是 `completed`,不出现在失败清单里。

### 2. 切片强制封顶(修 >8192 tokens)
- 问题:embedding 模型单次输入上限约 8192 token,某个 chunk 超了被拒。说明切片没有硬上限,很可能是整张 Excel 表或整页 OCR 文本被当成一个巨型 chunk,没走正常切分逻辑。
- 要做,两层:
  1. 切片层:给所有内容类型(正文、表格、OCR 文本)统一走带大小上限的切分。目标 400–512 token,硬上限设一个远低于 8192 的值(如 1000 token),超过就继续切;对没有天然分隔符的超长文本要能强制按长度切开。
  2. embedding 前兜底闸:在 `embedding.py` 发请求前,对每一条输入做 token 数(或长度近似)检查,只要仍超过安全阈值(如 8000),就地再切或截断,绝不让单条超限输入打到 Azure API。
- 注意:先定位那个超限 chunk 到底来自哪类文件(Excel 表 / OCR 页),确认它之前是不是根本没走切分逻辑,并在日志里打印命中兜底闸的 chunk 来源。
- 验收:重跑之前报 ">8192 tokens" 的文件,不再出现该错误;日志能看到超大块被拆分/兜底的记录。

---

## 第②批(修瞬时错误,让它们可自愈)

### 3. 429 限流:真·全局 + 尊重 Retry-After + 可重试
- 问题:已经做过退避重试,还在报 429。原因是限流不是跨进程真正全局——共享进程池 + 重文件独立子进程各自调 embedding、各自限流,汇总速率仍超 Azure 配额。
- 要做:
  1. 尊重 Retry-After:Azure 返回 429 时按响应头里的等待时间退避,而不是固定退避。
  2. 真全局限流:让所有 embedding 调用共用一个速率闸。stdlib 方案——集中到父进程统一发,或用 SQLite/文件锁做跨进程令牌桶,保证多进程汇总速率不超配额。不要每进程一个独立限流器。
  3. 429 归类为瞬时错误、可自动重试:失败后回到可重试队列,不要计入永久失败,也不要把文件标 `dead`。
- 验收:高并发批量下 429 大幅减少;偶发 429 能自动等待并重试成功,文件不因 429 变成终态失败。

### 4. timeout:调大重文件超时 + 确认流式构建生效
- 问题:超时的多是重文件(大 PDF/PPT/Excel),它们本来就慢。
- 要做:
  1. 把重文件(隔离子进程那条路)的超时上限调大(如 `TASK_TIMEOUT_SECONDS` 对重文件单独放宽)。
  2. 确认流式构建在重文件上真的生效:`write_chunk_batches()` 里不要先 `build_chunks_for_stage()` 一次性拿到整份 `List[DocumentChunk]` 再整体 embedding;应改成按文档单元(每页/每张表/每张幻灯片)边构建、边分批 embedding、边写 JSONL,处理完一段就释放内存。
  3. timeout 归类为可重试,不是终态。
- 验收:重文件不再因超时直接失败;内存峰值下降;超时文件能自动重试跑通。

---

## 第③批(运行方式,手动执行,非代码)
> 这一步是运行/运维动作,不需要改代码,但请在 README 或运行说明里补上:
1. worker/ingest 进程移出 PyCharm,放到独立终端运行:`python -m kb_admin.worker`(避免 IDE 自身 + 调试器 + web + worker 挤爆物理内存)。
2. 上述①②修完后,重跑那 20 个失败 + 5 个在途重文件,确认失败数降到个位数。

---

## 第④批(上生产前的安全加固)
### 5. 服务端强制审批
- 破坏性动作(删除文档/索引等)必须在**服务端**强制走人工审批,不能依赖前端或模型返回的 `needs_human` 标记。`requires_approval(action)` 对破坏性动作一律返回需要审批,忽略模型的判断。

### 6. 删除唯一性校验
- `resolve_delete_target`:按条件解析删除目标时,若匹配到 0 条或 >1 条,一律拒绝执行并报错,只有精确命中唯一目标才允许进入审批流程。

### 7. 输入边界
- 自然语言命令入口用 `ENABLE_NL_COMMAND` 特性开关控制,默认可关闭。
- `add_files`:对后缀白名单、单文件大小、文件数量、目录范围做上限校验,拒绝越界输入;文件哈希用分块读取(`hashlib` 流式),不要一次性读入整个文件。

---

## 通用要求
- 每项改动配单元测试或最小验证脚本,覆盖:无图文档→completed、超大 chunk→被拆分、429→重试成功、删除 0/多目标→被拒。
- 不要改动与本任务无关的逻辑;不确定的地方先在注释里标出假设,不要擅自扩大改动范围。
- 完成后给我一份简短的改动清单:改了哪些文件、每处解决什么问题、怎么验证。

